<!doctype html>
%html
  %head
    %meta{:charset => "utf-8"}
    
    <!-- Always force latest IE rendering engine or request Chrome Frame -->
    %meta{:content => "IE=edge,chrome=1",
          :"http-equiv" => "X-UA-Compatible"}

    <!-- Use title if it's in the page YAML frontmatter -->
    %title
      = data.page.title || "The Omegaverse"
    
    = stylesheet_link_tag "normalize", "all"
    = stylesheet_link_tag "vendor/jquery-ui"
    = yield_content :stylesheets

    = javascript_include_tag "vendor/jquery-1.7.1.min"
    = javascript_include_tag "vendor/require"
    = javascript_include_tag  "omega/navigation"
    = yield_content :javascripts
  
  %body{:class => page_classes}
    #navigation
      #primary_navigation
        = link_to "About",  "/wiki/The_Omegaverse:About"
        \/
        = link_to "Wiki",   "/wiki/Omegaverse"
        \/
        = link_to "Forums", "/wiki/Forum"
        \/
        = link_to "Stats",  "/stats.html", :only_path => false
        .clear_both

      #secondary_navigation
        = yield_content :secondary_navigation


    = yield
